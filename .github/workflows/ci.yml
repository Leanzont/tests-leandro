name: CI Test

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    name: test CI with this file
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: echo test 
        run: echo "Hi, this is a test"



